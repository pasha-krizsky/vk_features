from connection import Connection
from vk_data import VKData
c = Connection(VKData.APP_ID, VKData.LOGIN, VKData.PASSWORD)
# c.get_groups_amount(VKData.USER_ID)
# c.get_groups_names(VKData.USER_ID)
# c.get_groups_extended(VKData.USER_ID)
# c.get_groups_id(VKData.USER_ID)
# c.get_likes_in_groups(VKData.USER_ID, 10)
# c.get_likes_from_user(VKData.USER_ID, 10)
# c.get_friends_id(VKData.USER_ID)
# c.get_friends_amount(VKData.USER_ID)
